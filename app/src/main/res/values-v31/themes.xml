<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ (API 31) and up get better splash/edge handling -->
    <style name="Theme.PixelSnap" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowSplashScreenBackground">@color/md_theme_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
